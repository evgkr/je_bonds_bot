# Core
tinkoff-investments>=0.2.0
aiogram>=3.0,<4

# Data / parsing
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9

# Utils
tqdm>=4.66
pyxirr>=0.10

# gRPC (used directly in code via grpc.aio)
grpcio>=1.60

# Time zones (helps on Windows / slim Docker images)
tzdata>=2024.1
